#include <bits/stdc++.h>
using namespace std;
int main(){
	int a,b;
	cout<<"enter the value of a:-";
	cin>>a;
	cout<<"enter the value of b:-";
	cin>>b;
	if(a==b)
	cout<<"numbers entered by the user are equal";
	else
	cout<<"numbers entered by the user are not equal";
}
